<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ProjectPulse"
             xmlns:helpers="clr-namespace:ProjectPulse.Helpers"
             x:Class="ProjectPulse.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Company brand colors -->
            <Color x:Key="PrimaryColor">#dc3545</Color>
            <Color x:Key="SecondaryColor">#ffffff</Color>
            <Color x:Key="TertiaryColor">#f8f9fa</Color>
            <Color x:Key="PrimaryTextColor">#212529</Color>
            <Color x:Key="SecondaryTextColor">#6c757d</Color>
            
            <!-- Status colors -->
            <Color x:Key="SuccessColor">#28a745</Color>
            <Color x:Key="WarningColor">#ffc107</Color>
            <Color x:Key="DangerColor">#dc3545</Color>
            <Color x:Key="InfoColor">#17a2b8</Color>
            
            <!-- Converters -->
            <helpers:PercentageToDecimalConverter x:Key="PercentageToDecimalConverter" />
            <helpers:InverseBoolConverter x:Key="InverseBoolConverter" />
            <helpers:GreaterThanZeroConverter x:Key="GreaterThanZeroConverter" />
            <helpers:StatusToColorConverter x:Key="StatusToColorConverter" />
            <helpers:PriorityToColorConverter x:Key="PriorityToColorConverter" />
            
            <!-- Common Styles -->
            <Style x:Key="PrimaryButton" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource PrimaryColor}" />
                <Setter Property="TextColor" Value="{StaticResource SecondaryColor}" />
                <Setter Property="CornerRadius" Value="5" />
            </Style>
            
            <Style x:Key="SecondaryButton" TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource TertiaryColor}" />
                <Setter Property="TextColor" Value="{StaticResource PrimaryColor}" />
                <Setter Property="BorderColor" Value="{StaticResource PrimaryColor}" />
                <Setter Property="BorderWidth" Value="1" />
                <Setter Property="CornerRadius" Value="5" />
            </Style>
            
            <Style x:Key="CardBorder" TargetType="Border">
                <Setter Property="Stroke" Value="#e9ecef" />
                <Setter Property="StrokeThickness" Value="1" />
                <Setter Property="BackgroundColor" Value="White" />
                <Setter Property="StrokeShape" Value="RoundRectangle 10,10,10,10" />
            </Style>
            
            <Style x:Key="HeaderLabel" TargetType="Label">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
                <Setter Property="Margin" Value="0,10,0,5" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
